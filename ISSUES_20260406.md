# ambition_monkey 댓글/engage 이슈 (2026-04-06)

## 문제 요약

### 1. #52, #53 크론 미실행
- **원인**: 수동 테스트 시 time을 `14:02`, `14:47`로 변경 → 크론(정각)과 미매칭
- **증거**: `No pending posts for 2026-04-06 14:00`, `15:00`
- **결과**: first_reply, crosspost, engage가 크론 파이프라인을 안 탐
- **해결**: cmd_run()을 정각 매칭 → **시간대(hour) 범위 매칭**으로 변경

### 2. #54 crosspost 실패 (21:00)
- **원인**: `[Errno 2] No such file or directory: 'cmd.exe'`
- WSL에서 cmd.exe 경로를 못 찾음 (환경에 따라 간헐적)
- **해결**: cmd.exe 절대 경로 사용 (`/mnt/c/Windows/System32/cmd.exe`)

### 3. post_url 중복
- #52, #53, #54 모두 같은 URL: `DUj-iZ2GER9`
- **원인**: 프로필에서 `a[href*="/post/"]` 첫 번째를 잡는데, 캐시/타이밍 문제
- **해결**: 게시 전 URL 목록 저장 → 게시 후 새로 나타난 URL 찾기

### 4. first_reply 다른 글에 달릴 위험
- **원인**: `_post_first_reply`가 프로필에서 "최신 게시물"의 reply를 클릭
- 연속 게시 시 이전 글에 reply 달릴 수 있음
- **해결**: post_url 기반으로 해당 게시물 페이지에서 직접 reply

### 5. #54 engage는 성공
- collect 5개 + SQUID engage completed → engage 자체는 정상

## 수정 우선순위
1. ~~cmd_run() 시간대 범위 매칭 (가장 시급)~~ ✅ 해결 — hour 기반 매칭으로 변경
2. ~~cmd.exe 절대 경로~~ ✅ 해결 — `/mnt/c/Windows/System32/cmd.exe` 절대 경로 사용
3. ~~post_url 중복 해결~~ ✅ 해결 — 게시 전 URL 목록 저장 → 새 URL diff
4. ~~first_reply 정확도 개선~~ ✅ 해결 — post_url 직접 이동 후 reply
